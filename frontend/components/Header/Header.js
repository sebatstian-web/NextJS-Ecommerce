import TopBar from './TopBar';
import MenuWeb from './Menu';

const Header = () => {
  return (
    <div className="header">
      <TopBar />
      <MenuWeb />
    </div>
  );
};

export default Header;
